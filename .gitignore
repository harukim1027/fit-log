node_modules
dist
.env
.env.*
.env*.local
*.log
.expo

# 인증서 및 키 파일
*.pem
*.key
*.p12
*.p8
*.jks
*.mobileprovision

# EAS 빌드 시크릿
# eas.json 은 추적한다 — 빌드 프로필이 레포 밖에 있으면 CI 가 매번 새로
# 만들어야 하고, 그 과정에서 환경변수 블록이 통째로 날아간 적이 있다.
# 시크릿(Apple 자격정보)은 이 파일에 두지 않고 CI 가 submit 블록에만 주입한다.
# eas.json
**/secrets/
