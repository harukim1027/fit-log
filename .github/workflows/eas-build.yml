name: EAS Build & Submit
on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Platform'
        required: true
        default: 'ios'
        type: choice
        options: [ios, android, all]
