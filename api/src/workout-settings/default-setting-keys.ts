/**
 * 기구 설정 기본 항목 — **정본(single source of truth)**.
 *
 * 예전에는 이 목록이 클라이언트 상수(`SettingSelector.tsx`의
 * `DEFAULT_SETTING_KEYS`)로만 있었고, 코드에 박혀 있어 지울 수 없었다.
 * 기본 항목도 지울 수 있게 하려고 사용자 소유 데이터로 옮기면서 정본을
 * 서버로 가져왔다.
 *
 * 클라이언트에도 같은 목록이 남아 있지만 그쪽은 **오프라인 폴백 전용**이다
 * (조회 실패 + 캐시 없음일 때 표시만 한다). 두 벌이 어긋나면 복구가
 * 이상해지므로, 값을 바꿀 일이 생기면 **이 파일을 먼저 고치고** 클라이언트를
 * 맞춘다.
 *
 * 순서도 의미가 있다. `workout_setting_presets`에는 정렬 컬럼이 없어
 * `createdAt ASC`로 나열되므로, 여기 적힌 순서대로 INSERT 해야 화면 순서가
 * 이 순서와 같아진다.
 */
export const DEFAULT_SETTING_KEYS = [
  '시트높이',
  '등받이각도',
  '그립종류',
  '발판위치',
  '바높이',
  '인클라인각도',
] as const;
